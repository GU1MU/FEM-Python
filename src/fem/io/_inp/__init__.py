"""Private implementation package for the :mod:`fem.io.inp` facade.

The package intentionally has no re-exports.  Callers must use the stable
facade, while the parser, deck, builder, resolver, and input error plumbing
remain one implementation behind it.
"""
